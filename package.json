{
  "name": "dubsar-continuity",
  "version": "0.3.0-dev",
  "private": true,
  "type": "module",
  "description": "Local, offline project continuity and a read-only Workbench for coding agents.",
  "license": "MIT",
  "homepage": "https://github.com/kotnisofiane-bit/dubsar-agent-skills#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kotnisofiane-bit/dubsar-agent-skills.git"
  },
  "keywords": [
    "agent-skills",
    "ai-agents",
    "project-continuity",
    "project-memory",
    "claude-code",
    "codex",
    "cursor"
  ],
  "scripts": {
    "check:boundary": "node tools/check-packages.mjs --mode development",
    "check:conformance": "node tools/check-workbench-conformance.mjs --mode development",
    "check:package-release": "node tools/check-packages.mjs --mode release",
    "check:release": "node --input-type=module --eval \"const { spawnSync } = await import('node:child_process'); let failed = false; for (const args of [['tools/check-packages.mjs', '--mode', 'release'], ['tools/check-workbench-conformance.mjs', '--mode', 'release']]) { const result = spawnSync(process.execPath, args, { cwd: process.cwd(), stdio: 'inherit', windowsHide: true }); if (result.error || result.status !== 0) failed = true; } process.exitCode = failed ? 1 : 0;\"",
    "check:workbench": "node tools/check-workbench-runtime.mjs",
    "demo": "node tools/run-demo.mjs",
    "inventory": "node tools/check-packages.mjs --mode inventory",
    "workbench:manage": "node packages/dubsar-workbench-launcher/bin/dubsar-workbench-open.mjs --manage",
    "workbench:open": "node packages/dubsar-workbench-launcher/bin/dubsar-workbench-open.mjs --reviews",
    "workbench:ui": "node packages/dubsar-operator-cli/bin/dubsar.mjs ui --start . --domain project",
    "test": "npm run check:boundary && npm run check:workbench && npm run check:conformance && node tools/run-tests.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "acorn": "8.18.0"
  }
}
