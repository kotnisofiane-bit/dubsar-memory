{
  "name": "@dubsar/operator-cli",
  "version": "0.1.0-dev",
  "private": true,
  "type": "module",
  "description": "Local DUBSAR Workbench CLI with read-only inspection and explicit checkpoint writes.",
  "bin": {
    "dubsar": "./bin/dubsar.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
